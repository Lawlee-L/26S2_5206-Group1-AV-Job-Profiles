"""AV job-posting pipeline, v2: LLM-normalised records, clustered locally.

Entry point: run_pipeline_v2.py; with --db it also writes each run to SQLite in
the tables and columns of schema.mysql.sql (store.py). v1 (rule-based,
English-only) lives in Experiment 2.
"""
