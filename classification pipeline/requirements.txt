# AV job-posting pipeline v2. Tested on Python 3.12 (macOS / Apple Silicon).
# Model calls go through OpenRouter (OPENROUTER_API_KEY); everything else runs locally.

numpy>=1.26,<3
pandas>=2.0
scikit-learn>=1.5          # HDBSCAN, TF-IDF, dedupe
sentence-transformers>=3.0 # MiniLM embeddings; pulls in torch, weights download once
umap-learn>=0.5.6          # dimensionality reduction before HDBSCAN
openrouter>=1.2            # model client
tiktoken>=0.7              # --dry-run token counts
jsonschema>=4.0            # validating model replies
python-dotenv>=1.0        # loads OPENROUTER_API_KEY from .env
